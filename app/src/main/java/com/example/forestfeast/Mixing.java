package com.example.forestfeast;

import android.graphics.Bitmap;
import android.util.Log;

public class Mixing extends Figure {
    public Mixing(int x, int y, Bitmap[] frames, int width, int height, int screenWidth, int screenHeight) {
        super(x, y, frames, width, height, screenWidth, screenHeight);
    }
}
